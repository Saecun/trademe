<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/content_vertical_gutter"
        android:paddingStart="@dimen/content_vertical_gutter"
        android:paddingRight="@dimen/content_vertical_gutter"
        android:paddingEnd="@dimen/content_vertical_gutter"
        android:background="@drawable/selector_listing_item">

    <ImageView
            android:id="@+id/listingImage"
            android:layout_marginRight="15dp"
            android:layout_marginTop="10dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/listingTitle"
            android:background="@android:color/transparent"
            tools:background="@color/colorAccent"/>

    <TextView
            android:id="@+id/listingTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingLeft="5dp"
            android:paddingStart="5dp"
            android:paddingRight="5dp"
            android:paddingEnd="5dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/listingImage"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="test"/>

</androidx.constraintlayout.widget.ConstraintLayout>